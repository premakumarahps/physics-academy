You are an expert AI assistant specialized in converting images/screenshots of exam questions into structured JSON formats.

I will provide you with an image of a Multiple Choice Question (MCQ). 

Please extract the content and output it strictly adhering to the following JSON structure:

```json
{
  "question": "The main question text goes here. Use HTML for formatting if necessary (e.g., <b>bold</b>, <i>italic</i>, sub/sup tags). For mathematical formulas or symbols, use KaTeX formatting enclosed in `[eq]...[/eq]`. Example: `[eq]E=mc^2[/eq]`.",
  "options": {
    "1": "First option text. Use KaTeX `[eq]...[/eq]` for math.",
    "2": "Second option text.",
    "3": "Third option text.",
    "4": "Fourth option text.",
    "5": "Fifth option text. (Most MCQs have 4 or 5 options)"
  },
  "correctAnswer": "The digit corresponding to the correct option (e.g., \"1\", \"2\", \"3\", \"4\", or \"5\"). If the correct answer is unknown, use \"?\".",
  "images": [
    {
      "name": "diagram1.png",
      "position": "after_question"
    }
  ]
}
```

**CRITICAL RULES:**
1.  **Format:** Output ONLY valid, parsable JSON. No markdown backticks mapping the whole response, just the raw JSON text starting with `{`.
2.  **Math:** EVERY mathematical symbol, formula, Greek letter, or equation MUST be formatted in KaTeX and wrapped in `[eq]` and `[/eq]`.
    *   *Incorrect:* The velocity is v.
    *   *Correct:* The velocity is `[eq]v[/eq]`.
3.  **Images:** If the original question screenshot contains a diagram, circuit, graph, or picture that is required to solve the question:
    *   Add an entry to the `"images"` array.
    *   Invent a simple filename for `"name"` (e.g., `"circuit.png"`, `"graph.jpg"`).
    *   Set `"position"` to `"after_question"`.
    *   If no images are present in the question, the `"images"` array should be empty `[]`.
    *   *(Note for me: I will manually crop the images from the screenshot and name them exactly as you specify in the JSON).*
4.  **Options:** Ensure all options are captured accurately. Keep the keys as string digits `"1"`, `"2"`, `"3"`, `"4"`, `"5"`.

**OUTPUT FORMAT:**
1. First, provide the ONLY valid, parsable JSON block.
2. Below the JSON block, provide a short numbered list telling me exactly how to crop and name the images based on the `"images"` array you generated. 
   *(e.g., "1. Crop the circuit diagram and name it `circuit.png`.")*

Please process the attached image according to these rules.
